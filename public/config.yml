backend:
  name: git-gateway

media_folder: "media_folder"

collections:
  - name: "projects"
    label: "Project"
    folder: "projects"
    create: true
    fields:
      - {label: "Name", name: "name", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}
